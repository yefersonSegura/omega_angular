{
  "$schema": "http://json-schema.org/draft-07/schema",
  "title": "eslint-then dev server",
  "description": "Runs ESLint, then delegates to the inner dev-server target. All other options are forwarded as-is (same as @angular/build:dev-server).",
  "type": "object",
  "properties": {
    "delegateProject": {
      "type": "string",
      "description": "Workspace project name for the inner target (eslint-then)."
    },
    "delegateTarget": {
      "type": "string",
      "description": "Target name of the inner dev-server (eslint-then)."
    }
  },
  "additionalProperties": true,
  "required": []
}
